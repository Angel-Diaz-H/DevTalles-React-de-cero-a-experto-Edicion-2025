const firstName = 'Angel';
const lastName = "O'Neal";

// Estas concatenaciones se utilizan en React.
const fullName = `
    El nombre completo es:
    ${firstName} ${lastName}
    `;

// console.log(firstName + ' ' + lastName);

console.log(fullName);